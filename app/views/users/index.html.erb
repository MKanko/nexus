<h3>All Users In Database</h3>

<div class='ui blue segment'>
    <ul>
        <% @users.each do |user| %>
            <li><%= link_to user.username, user_path(user) %> <%= link_to 'Delete User', user_path(user), method: :delete, data: { confirm: "Are you sure?" } %></li>
        <% end %>
    </ul>
<div>


    

    
     