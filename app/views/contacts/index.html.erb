<h2><%= current_user.username %> Contacts:</h2>

    <ul>
        <% @contacts.each do |con| %>
            <li><%= link_to con.name, contact_path(con) %></li>
        <% end %>
    </ul>

    